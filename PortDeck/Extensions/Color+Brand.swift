import SwiftUI

extension Color {
    static let recallPrimary = Color.blue
    static let recallAccent = Color.indigo
    static let recallRecording = Color.red
    static let recallSuccess = Color.green
    static let recallWarning = Color.orange
}
